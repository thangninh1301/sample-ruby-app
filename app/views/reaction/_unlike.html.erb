<%= link_to image_tag(link_by_icon(@micropost.get_reaction(current_user.id).icon_id), size: "20x20"),
            reaction_path(:id => @micropost.get_reaction(current_user.id), :micropost_id => @micropost.id),
            :method => :delete, remote: true %>
&nbsp
